!define TEST_RUNNER (${FITNESSE_ROOTPATH}/fitnesse/rubyslim/run_ruby_slim.rb)
!define COMMAND_PATTERN (ruby -I ${FITNESSE_ROOTPATH}/fitnesse/rubyslim -I ${FITNESSE_ROOTPATH}/ruby %m)

!define SLIM_PORT (${RUBY_SLIM_PORT})

!contents